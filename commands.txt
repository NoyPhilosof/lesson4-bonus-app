cd bonus-app
git checkout -b gh-pages
helm package ../bonus-app
helm repo index ../bonus-app
git add .
git commit -m 'upload files to gh-pages branch'
git push
configure gh-pages settings using web browser
helm repo add lesson4-bonus-app https://noyphilosof.github.io/lesson4-bonus-app/
helm repo update
helm upgrade -i bonus-app lesson4-bonus-app/bonus-app
